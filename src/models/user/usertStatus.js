export default class UserStatus {
  id = '';
  name = '';

  /**
   *
   * @param {string} id
   * @param {string} name
   */
  constructor(id, name) {
    this.id = name;
    this.name = name;
  }
}
